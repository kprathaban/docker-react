sudo: required

services:
  - docker

before_install:
  - docker build -t karthiksaran/docker-react -f Dockerfile.development

script:
  - docker run karthiksaran/docker-react npm run test -- --coverage