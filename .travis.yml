sudo: required

services:
  - docker

before_install:
  - docker build -t karthiksaran/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true karthiksaran/docker-react123 npm run test -- --coverage